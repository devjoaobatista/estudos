const numeros = [1, 2, 3, 4, 5];
const dobro = MultiPor2 => MultiPor2 * 2;
console.log(numeros.map(dobro));

const nomes = ["Ana", "Bia", "Gui", "Lia", "Rafa"]
const primeiraLetra = texto => texto[0]
console.log((nomes.map(primeiraLetra)))